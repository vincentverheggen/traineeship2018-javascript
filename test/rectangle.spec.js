/* import Rechtangle from '../src/rechtangle';

describe('Rechtangle', () => {
    it('Rechtangle', () => {
        let shape = new Shape("Red", 0, 5);
        shape.move(10, 15)
        expect(shape.x).toEqual(10);
        expect(shape.y).toEqual(15);
    });
 });
 */